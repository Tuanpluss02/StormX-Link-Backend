{
    "version": 2,
    "buildCommand": "yarn build",
    "installCommand": "yarn install --frozen-lockfile",
    "outputDirectory": "dist",
    "builds": [
        {
            "src": "dist/main.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dist/main.js"
        }
    ]
}